#A simple example of a lambda function that squares a number:

square = lambda x: x**2

# call the lambda function
n=int(input("Number:"))
result = square(n)

# print the result
print(result)
